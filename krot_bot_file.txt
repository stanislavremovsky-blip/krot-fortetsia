import os
import logging
from telegram import Update
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes
from google import genai
from google.genai import types

# =======================================================================
# !!! КЛЮЧІ БУДУТЬ ЗАВАНТАЖЕНІ ЗІ ЗМІННИХ СЕРЕДОВИЩА RENDER !!!
# =======================================================================
# Це гарантує, що ключі будуть доступні, оскільки вони вимагаються Webhooks
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")

# Налаштування логування для діагностики
logging.basicConfig(format='%(asctime)s - %(name)s - %(levelname)s - %(message)s', level=logging.INFO)
logger = logging.getLogger(__name__)

# -----------------------------------------------------------------------
# ВАШ ФІНАЛЬНИЙ МАЙСТЕР-ПРОМПТ (Системна інструкція для Майстра Гри)
# -----------------------------------------------------------------------
SYSTEM_PROMPT = """
Ти є Майстер Гри (МГ), офіцер контррозвідки, суворий, об'єктивний та емоційно нейтральний. 
Твоє головне завдання — керувати грою "Кріт у Фортеці", суворо дотримуючись усіх 6-ти правил нижче. 
Спілкуйся українською мовою. Жодного разу не виходь з ролі МГ, не надавай неігрової інформації, не коментуй дії гравця. 
Використовуй **жирний шрифт** для позначення назви раунду та імені підозрюваного.

# 📋 Правила Гри: "Кріт у Фортеці"
Майстер Гри (МГ): ШІ (Модель).
Гравець: Користувач (Контррозвідник).

1. Мета Гри
Мета гравця — виявити одного російського шпигуна (Крота) серед 10 підозрюваних співробітників за 10 раундів.

2. Умови та Локація
Локація: МГ щоразу генерує нову локацію розслідування на свій вибір (критично важлива українська установа, орган державної влади, орган місцевого самоврядування тощо). 
Персонажі: 10 співробітників, які мають доступ до конфіденційної інформації. Тільки один із них є Кротом. Перед першим раундом МГ публікує список усіх підозрюваних співробітників із зазначенням їхніх посад.

3. Внутрішня змінна (КРИТИЧНО)
Перед початком гри МГ визначає, але не публікує:
а) ім'я Крота, 
б) його мотив (шантаж, гроші, ідеологія, зв'язки), 
в) а також механізм прикриття.
МГ зберігає цю інформацію таємно від гравця ЯК ВНУТРІШНЮ ЗМІННУ І НЕ ВІДОБРАЖАЄ ЇЇ У ВИВОДІ. Ця інформація розкривається лише після перемоги/програшу гравця.

4. Механіка Раундів
Гра складається максимум із 10 раундів.
На початку гри: МГ генерує локацію та список 10 підозрюваних.
Кожен Раунд (1-10): Гравець обирає одного підозрюваного для опитування.

Анкета: МГ надає детальну Анкету на обраного підозрюваного (ПІБ, посада, вік, стаж, попередня робота, сімейний стан, уразливості/конфлікти).
Опитування: Гравець може поставити підозрюваному три запитання. МГ відповідає від імені персонажа, відображаючи його характер, знання та емоційну реакцію.

4.5. Інструкція щодо Лічильника Запитань (КРИТИЧНО!):
4.5.1. МГ повинен активно відстежувати кількість запитань, поставлених Гравцем у поточному раунді (Раунд X).
4.5.2. Після кожної відповіді підозрюваного, МГ повинен перевірити, чи вичерпано ліміт (3 запитання).
4.5.3. Якщо ліміт НЕ вичерпано (залишилося 2 або 1 запитання), МГ повинен завершити свій вивід від імені підозрюваного чітким нагадуванням: "Контррозвідник, у вас залишилося [Кількість] запитань для цього підозрюваного. Ваше наступне запитання?"
4.5.4. Якщо ліміт ВИЧЕРПАНО (поставлено 3 запитання), МГ повинен завершити раунд, вивівши: "Раунд [X/10] завершено. Час для наступного кроку. Виберіть: 1) Назвати ім'я Крота, 2) Розпочати наступний Раунд і обрати нового підозрюваного."

5. Умови Перемоги та Програшу
Перемога: Гравець у будь-який момент може назвати ім'я Крота. Якщо він вгадує, гра завершується Перемогою.
Програш: Гравець назвав ім'я і помилився.

6. Принцип Нейтральності Майстра Гри
Основна функція Майстра Гри — це створення об'єктивного та послідовного світу, який реагує на дії гравця відповідно до внутрішньої логіки та характеристик персонажів, а не бажання МГ "допомогти" чи "завадити" гравцеві.

7. Формат Першого Виведення:
Після отримання команди 'СТАРТ' від гравця, МГ повинен спершу згенерувати внутрішні змінні (Кріт, Мотив, Прикриття) та вивести:
1. Назву локації.
2. Пронумерований список 10 підозрюваних із зазначенням посади.
3. Заклик до гравця: "Розпочинаємо Раунд 1/10. Оберіть ПІБ підозрюваного для опитування."
"""
# -----------------------------------------------------------------------

# Ініціалізація клієнта Gemini
# os.environ.get автоматично шукає ключ GEMINI_API_KEY 
client = genai.Client() 
user_chats = {} 

async def start_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обробляє команду /start і ініціалізує нову гру."""
    chat_id = update.effective_chat.id
    
    # Створення нової сесії чату з моделлю
    try:
        chat = client.chats.create(
            model="gemini-2.5-flash",
            system_instruction=SYSTEM_PROMPT
        )
        user_chats[chat_id] = chat
        
        initial_response = chat.send_message("СТАРТ ГРИ. Сформуй внутрішні змінні і виведи початковий опис згідно з Правилом 7.")
        await update.message.reply_text(initial_response.text)
        logger.info(f"New game started for chat_id: {chat_id}")

    except Exception as e:
        logger.error(f"Error starting chat: {e}")
        await update.message.reply_text(f"Виникла помилка при запуску гри: {e}. Перевірте ваші ключі API.")

async def handle_message(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обробляє всі повідомлення гравця в середині гри."""
    chat_id = update.effective_chat.id
    user_message = update.message.text
    
    if chat_id not in user_chats:
        await update.message.reply_text("Будь ласка, почніть нову гру командою /start.")
        return

    chat = user_chats[chat_id]
    
    try:
        response = chat.send_message(user_message)
        await update.message.reply_text(response.text)
        
        # Перевірка на завершення гри
        if "Перемога" in response.text or "Програш" in response.text:
            del user_chats[chat_id]
            await update.message.reply_text("Гра завершена. Почніть нову справу за допомогою /start.")
            
    except Exception as e:
        logger.error(f"Error handling message: {e}")
        await update.message.reply_text(f"Виникла технічна помилка: {e}. Спробуйте /start знову.")

def main() -> None:
    """Запускає бота в режимі Webhooks для Render."""
    
    if not TELEGRAM_BOT_TOKEN:
        logger.error("TELEGRAM_BOT_TOKEN не знайдено.")
        return

    # Ініціалізація програми Telegram
    application = Application.builder().token(TELEGRAM_BOT_TOKEN).build()

    # Реєстрація обробників
    application.add_handler(CommandHandler("start", start_command))
    application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message))
    
    # -----------------------------------------------------------
    # КРИТИЧНО: Налаштування Webhooks для Render
    # -----------------------------------------------------------
    
    # Render надає порт та зовнішній URL через змінні середовища
    PORT = int(os.environ.get('PORT', 8080))
    WEBHOOK_URL = os.environ.get('RENDER_EXTERNAL_URL') 

    if WEBHOOK_URL:
        logger.info(f"Starting Webhook mode on port {PORT}")
        
        # Налаштування параметрів вебхука
        application.run_webhook(
            listen="0.0.0.0",
            port=PORT,
            url_path=TELEGRAM_BOT_TOKEN,
            webhook_url=f"{WEBHOOK_URL}webhook/{TELEGRAM_BOT_TOKEN}", # Додано '/webhook/' для чистоти
            # Примітка: application.run_webhook автоматично стартує сервер
        )
    else:
        # Режим Polling (якщо запускається локально, наприклад, у Termux)
        logger.warning("RENDER_EXTERNAL_URL не знайдено, запускаємо в режимі Polling (для тестування).")
        application.run_polling(poll_interval=3)

if __name__ == "__main__":
    main()
